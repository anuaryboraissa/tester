class RegisteredUser {
  final bool? tinNumberValid;
  final bool? accountValid;
  final bool? passwordValid;
  final bool? passwordMatch;

  RegisteredUser(
      {this.tinNumberValid,
      this.accountValid,
      this.passwordValid,
      this.passwordMatch});

//check for every field if is changed
}
